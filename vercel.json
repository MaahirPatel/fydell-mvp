{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm run build:sim && next build",
  "crons": [
    {
      "path": "/api/cron/sandbox-cleanup",
      "schedule": "0 3 * * *"
    }
  ]
}
